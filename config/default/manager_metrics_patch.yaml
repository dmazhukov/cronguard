# Bind the metrics endpoint to all interfaces so the Service can reach it.
# Plain HTTP — see metrics_service.yaml for why the port is not named "https".
- op: add
  path: /spec/template/spec/containers/0/args/0
  value: --metrics-bind-address=:8080
